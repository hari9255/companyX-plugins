{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | \"${CLAUDE_PLUGIN_ROOT}/bin/format-java.sh\""
          }
        ]
      }
    ]
  }
}
